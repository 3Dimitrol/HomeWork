package home_work_1;

//import java.util.Scanner;

public class Work1_3_BitwiseOperations {
    public static void main(String[] args) {

       /*  Scanner enter = new Scanner(System.in);
        System.out.println("Введите число");
      */  float con = 42.5f;

        if ( con >= 100 ) {


          /*  if (enter.hasNextFloat()) {
                float numberF = enter.nextFloat();

                float oper1 = numberF ~ con;
                float oper2 = numberF ^ con;

                float oper3 = con >> numberF;
                float oper4 = con << numberF;

                System.out.println("Результаты побитовых операций: " +
                '\n' + "Побитовое NOT: " + oper1 +
                '\n' + "Побитовое XOR: " + oper2 +
                '\n' + "Побитовое сдвиг вправо: " + oper3 +
                '\n' + "Побитовое сдвиг влево :" + oper4);

            } else if (enter.hasNextInt()) {
                int numberN = enter.nextInt();

                int oper5 = numberN ~ con;
                int oper6 = numberN ^ con;

                int oper7 = con >> numberN;
                int oper8 = con << numberN;

                System.out.println("Результаты побитовых операций: " +
                '\n' + "Побитовое NOT: " + oper5 +
                '\n' + "Побитовое XOR: " + oper6 +
                '\n' + "Побитовое сдвиг вправо: " + oper7 +
                '\n' + "Побитовое сдвиг влево :" + oper8);
            } else {

            }
*/
        } else {
            System.out.println("Побитовые операции, не работают с типами \"float\" и \"double\"." + "\nДля коректной работы с побитовыми операторами лучше использовать целочисленные типы.");
        }
    }
}
